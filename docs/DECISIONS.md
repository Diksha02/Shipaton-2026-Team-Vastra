# DECISIONS

Append-only. One line per decision, newest at the bottom. Never edit or delete
an existing line — if a decision is reversed, append the reversal.

Format: `YYYY-MM-DD · <ticket or scope> · <decision> — <why>`

---

2026-08-01 · repo · PROJECT.md lives at the repo root rather than `docs/PROJECT.md` as §3 states — it is the session-load file, and root is where it will actually be found. Layout otherwise follows §3.
2026-08-01 · repo · pnpm workspaces + Turborepo, per §3. Node >=22 enforced via `engines`.
2026-08-01 · repo · `node-linker=hoisted` in `.npmrc` — Expo/React Native native module resolution is unreliable under pnpm's default symlinked store.
2026-08-01 · tsconfig · Enabled `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes` on top of `strict`. §7 bans `any` and `!`, which is only enforceable in practice with these two on.
2026-08-01 · env · Local Postgres and Redis bind to `127.0.0.1` explicitly, not Docker's default `0.0.0.0` publish form, so local infra is never reachable from the LAN.
2026-08-01 · env · Redis runs with `maxmemory-policy noeviction` — BullMQ loses jobs if Redis evicts job keys under pressure.
2026-08-01 · env · `.env.example` separates server-only secrets from `EXPO_PUBLIC_*` client-safe values, with the rule stated inline. Prevents a provider key being bundled into the app.
2026-08-01 · repo · Web app stays OUT of V1 per §2.3, confirmed with Ujjwal after a conflicting request. Expo already produces native iOS and Android binaries, so "native both platforms" is satisfied without a fourth target or a second RevenueCat billing path.
2026-08-01 · shared/db · Intra-package imports are extensionless, not `./x.js`. `moduleResolution: Bundler` permits it, and drizzle-kit's CJS loader cannot resolve the NodeNext `.js` form against `.ts` sources.
2026-08-01 · db · Local Postgres is published on host port 55432, not 5432 — a native Postgres install already holds 5432 on this machine. Container-internal port is unchanged.
2026-08-01 · db · drizzle.config.ts loads the repo-root `.env` explicitly via dotenv; drizzle-kit spawns its own process and does not inherit workspace env.
2026-08-01 · db · Verified against live Postgres: the partial unique index admits many user photos with null external_id and rejects a duplicate (source, brand, external_id). Invariant holds, not just the DDL.
2026-08-01 · design · Added `packages/design`, a fourth package beyond the three listed in §3. Not a stack substitution — tokens are app-only and do not belong in `shared` (which §3 scopes to zod schemas shared with the API). Flag if this should be folded into `shared` instead.
2026-08-01 · design · Type: Instrument Serif (display) + Inter (UI), both OFL. Serif is reserved for display sizes; body copy and controls are never set in it.
2026-08-01 · design · Warm stone neutrals rather than cool slate — garment photography is rarely colour-neutral and fights a cool canvas.
2026-08-01 · design · Light and dark are peers; the app follows the OS setting. Neither is derived from the other.
2026-08-01 · design · **Primary actions are monochrome; the accent is reserved exclusively for Plus and the paywall.** Colour therefore signifies premium, and the purchase surface is the only chromatic moment in the app. Directly serves §6.
2026-08-01 · design · Elevation uses borders and background separation before shadows — heavy drop shadows read as dated and look worse over garment photography.
2026-08-01 · design · Text colour ratios verified against WCAG AA (textTertiary 4.6:1 light / 5.9:1 dark). `textDisabled` intentionally fails AA so disabled controls cannot read as active.
2026-08-01 · mobile · Expo SDK 57 / RN 0.86 / React 19.2, scaffolded via create-expo-app so versions match the SDK rather than being guessed.
2026-08-01 · mobile · Metro needs explicit monorepo wiring under pnpm: `watchFolders` on the workspace root, explicit `nodeModulesPaths`, and `disableHierarchicalLookup` — without the last one Metro can resolve two copies of React, which surfaces as "Invalid hook call" rather than a resolution error.
2026-08-01 · mobile · Reanimated 4 moved its babel transform to `react-native-worklets/plugin`.
2026-08-01 · mobile · `userInterfaceStyle: automatic` — the app follows the OS, with a manual override available in Profile. Light and dark are peers.
2026-08-01 · mobile · Garment imagery is stubbed with flat colour swatches derived from the item's colour token. Deliberate: it lets grid rhythm, type and spacing be judged before any photography or R2 bucket exists, and reads as an intentional placeholder rather than a broken image.
2026-08-01 · mobile · react-native-web is included for browser-based design review only. This is NOT the web app that §2.3 excludes — no web target ships, and no product surface depends on it.
2026-08-01 · mobile · Paywall obeys §6 literally: contextual trigger, loss framing (the at-risk outfit is rendered, not described), a real always-visible close button, no countdowns, no pre-ticked options.
2026-08-01 · naming · PROJECT.md §1 locked the name to **Vastra** (वस्त्र, "garment"), bundle id `app.vastra`. Renamed workspace scope `@wardrobe/*` → `@vastra/*`, app.json identity, and the local Postgres database/user/containers. Done now because a scope rename is mechanical today and invasive once CI, EAS and store listings reference it.
2026-08-01 · naming · ⚠️ §6 still specifies product ids `wardrobe_plus_monthly` / `wardrobe_plus_annual`. Left unchanged — those are spec-locked store identifiers. **Flagged for Ujjwal:** store product ids are effectively permanent once created in App Store Connect / Play Console, so if they should read `vastra_plus_*` it must be decided before T03.
2026-08-01 · mobile IA · Landing tab is now Today (discovery: brand moments, trending, locked OOTD and brand-connection teasers), not the wardrobe grid. A personal inventory is a tool you go into, not a homepage.
2026-08-01 · mobile IA · Wardrobe became Studio — a centre outfit stage with per-layer swipeable carousels below it. The full inventory grid moved to a secondary route (`/wardrobe-grid`). Building an outfit is the daily job; browsing everything you own is occasional, so the daily job gets the tab.
2026-08-01 · mobile IA · Discover tab removed; its catalogue surface (F9/F10) is now the Trending row on Today. Four tabs: Today, Studio, Outfits, You.
2026-08-01 · mobile · OOTD and brand connections promoted from Profile to the Today feed, still locked per §2.2 — illustrative preview, Coming soon badge, Notify me only, no Buy/Unlock CTA anywhere.
2026-08-01 · mobile · Added PhoneFrame: on web the app is constrained to 430px and centred. react-navigation's tab bar mispositions across a desktop viewport, which made browser review misleading. No-op on native.
2026-08-01 · studio · Rebuilt against a tablet reference (centre stage flanked by eight category columns). Eight columns cannot fit 430px and §2.3 cuts tablet layouts, so the layer rail switches which single carousel is shown. Interaction model preserved; only simultaneity lost.
2026-08-01 · studio · Garments now arrange as a figure (open-coat panels flanking a torso, legs, shoes, accents pinned aside) on a lit arch and podium, rather than as stacked bands. A silhouette reads as an outfit at a glance; equal bands read as a chart.
2026-08-01 · studio · Added Shuffle. Accents (hat/bag/accessory) are randomly omitted ~50% of the time — a random hat on every outfit gets old fast. Shuffle is also the single most demoable interaction for the §1 two-minute video.
2026-08-01 · studio · The layer rail doubles as outfit progress: a filled layer carries a dot, so the user can see what is still empty without leaving the stage.
2026-08-01 · design · Added a `micro` (9pt) type token. Studio's icon-button captions had been setting fontSize inline, which is exactly what the token system exists to prevent.
2026-08-01 · brand · Wordmark placement: a centred Instrument Serif "Vastra" masthead with a tracked "by you" beneath, on the Today screen only. Centred serif mastheads are the oldest and strongest pattern in fashion publishing; repeating a wordmark on every screen is what stops it meaning anything.
2026-08-01 · assets · Placeholder garment photography added: 21 Unsplash-licensed images in `apps/mobile/assets/garments`. Items in mock data are named after what each photo actually shows, so the grid reads as one coherent wardrobe instead of captions contradicting their pictures. See docs/ASSETS.md — these must be replaced before T34.
2026-08-01 · assets · Every download was visually verified via a generated contact sheet before use. Search-supplied alt text did not reliably match the real photo, and 9 of 30 were discarded: visible third-party trademarks (Nike, Levi's, D&G), whole-outfit flat lays where one garment was expected, people rather than products, and slogan text on garments.
2026-08-01 · mobile · Added `types/assets.d.ts`. Expo SDK 57 ships no ambient declarations for `*.jpg` imports, so every image import failed typecheck. Kept separate from the generated `expo-env.d.ts`, which is marked not to be edited.
2026-08-01 · studio · With real photography the stage became a styled collage rather than a literal silhouette — a jacket photograph does not crop into a 26px strip. Body pieces hold the centre column; outerwear, hat, bag and accessory pin around it.
2026-08-01 · studio · Adopted FlashList (Shopify) for the layer carousels — recycles rows rather than mounting every item, which is what holds 60fps once a wardrobe has hundreds of garments. §3 states no list library, so this is an addition, not a substitution. Note FlashList v2 removed `estimatedItemSize`.
2026-08-01 · studio · Outfit state moved to Zustand (`src/store/outfit.ts`) holding ids, not item objects. `setLayer` replaces exactly one key and bails when unchanged; garment panels, rail buttons and the carousel each subscribe to their own slice via selectors. Tapping a shoe re-renders the shoe panel and two carousel cards — not the screen.
2026-08-01 · studio · Layer vocabulary stays `footwear`/`headwear`/`accessory` (the ItemCategory enum in @vastra/shared) rather than `shoes`/`hat`/`glasses`. One vocabulary from database to UI means no mapping layer to drift.
2026-08-01 · studio · Garment swaps use Moti `AnimatePresence` with `exitBeforeEnter`: fade-out → slide-up → fade-in at 160ms. The outgoing garment leaves before the incoming arrives, rather than the two crossfading through each other.
2026-08-01 · studio · Haptics on selection (selectionAsync) and on shuffle (impactAsync medium). Guarded by `Platform.OS !== 'web'` — expo-haptics throws on web rather than no-opping.
2026-08-01 · assets · Converted placeholders JPEG → WebP at two sizes (800px display, 240px thumb). 713 KB → 383 KB, **46% smaller while adding thumbnails**. Carousels load the thumb; stage, grid and hero load the display.
2026-08-01 · stack · ⚠️ Proposed Supabase/Firebase backend, Supabase/Firebase Auth, and AWS S3 were NOT adopted — §3 specifies NestJS + Neon/Drizzle, Clerk, and Cloudflare R2, and says "do not substitute without asking". The NestJS API and Drizzle schema are already built and green. Raised with Ujjwal; unresolved.
2026-08-01 · stack · ⚠️ §3 specifies Reanimated 3; Expo SDK 57 pins Reanimated 4.5.1 and moved the babel transform to `react-native-worklets/plugin`. Deviation is forced by the SDK, not chosen.
2026-08-01 · product · Studio shows a neutral anonymous figure, not the user's likeness; seeing yourself in an outfit is what Try-On is for. Economically load-bearing: the figure is free and local, a FASHN render is ~$0.075, so browsing costs nothing and try-on stays a deliberate act. Parked with open questions FA-1..FA-5 in docs/TASKS.md — not scheduled, nothing built.
2026-08-01 · providers · Try-on provider changed from **FASHN.ai to Google Vertex AI `virtual-try-on-001`** (Imagen family) at Ujjwal's direction. Documented only — no code written, no account created. `TryonProvider` in packages/providers keeps the swap to one adapter file. ⚠️ PROJECT.md §3 still names FASHN and needs amending.
2026-08-01 · providers · Body proportions will come from **MediaPipe Pose Landmarker** (Google AI Edge), on-device: free, no API key, and body data never leaves the phone. Gives proportions not absolute sizes, so height is the scale reference and the user confirms. Answers FA-1.
2026-08-01 · product · FA-2 decided: **2D parametric silhouette**, not 3D. A rigged figure with cloth draping needs a 3D library §3 does not name, plus garment assets we cannot derive from photographs. 3D stays open post-V1.
2026-08-01 · providers · Considered and not chosen: fal.ai as a multi-model gateway (one SDK over FASHN/Kolors/IDM-VTON/FLUX). Would have made T00 a bake-off rather than a bet. Ujjwal chose a single first-party Google relationship instead.
2026-08-01 · brand · App icon: a single Instrument Serif "V" in cream on near-black, generated from the actual brand TTF so the icon and the in-app masthead are the same letterform. One letter deliberately — wordmarks beyond ~3 characters stop being legible at 48px, which is the size that decides whether anyone taps.
2026-08-01 · brand · Splash has light and dark variants (`splash-icon.png` / `splash-icon-dark.png`) wired through the expo-splash-screen plugin, because the app follows the OS theme. A single light splash would flash white on a dark phone.
2026-08-01 · mobile · LoadingScreen continues the native splash — same mark, same background — so the handover from OS to app is invisible. Uses the mark as an image because it renders before fonts resolve. A breathing rule rather than a spinner: a spinner reads as "this might fail".
2026-08-01 · mobile · ThemeProvider now wraps the loading state too. Returning null before it mounted flashed a light background on dark devices, which is the exact seam the branded loading screen exists to hide.
2026-08-01 · studio UX · Replaced the vertical icon rail with a horizontal CategoryStrip above the carousel. Each category shows **the garment currently chosen for it** rather than an abstract glyph — a `square` icon does not say "top" to anyone, a photo of your own jumper does — plus a text label. Seven unlabelled targets down a phone edge is expert UI.
2026-08-01 · studio UX · Stage controls moved off the stage into a labelled row: "Surprise me" / "Start over" / "Save". Icon-only buttons are expert UI, and "Reset" reads as destructive.
2026-08-01 · studio UX · Copy rewritten for a first-time reader: "Choose a top", "Saved outfits", "3 of 5 spaces used", "Pick something to start". Accessibility labels added to every control.
2026-08-01 · mobile · Loading screen holds for a 900ms minimum. Fonts are bundled and resolve in about a frame, so the brand moment was never actually seen. Launch is the one moment the brand has undivided attention; a launch that flickers straight into a grid feels cheaper than one that settles.
2026-08-01 · studio UX · Redesigned for calm. Removed the saved-outfits strip entirely (it duplicated the Outfits tab), leaving five bands, two of which are a single line of text: stage, quiet actions, categories, garments, one primary button. **Removing things was the fix, not restyling them.**
2026-08-01 · studio UX · Category strip reverted from thumbnails to text pills. Thumbnails were more literal but put two rows of photographs directly above each other with nowhere for the eye to rest. The garments below are the images; the category row is the index. A filled category carries a dot — enough.
2026-08-01 · studio UX · Garment cards enlarged 82 → 108px, with the centred card at full scale and neighbours dimmed to 0.55 and scaled to 0.84. The row now reads as one thing being considered with alternatives either side, rather than a grid of equal options.
2026-08-01 · studio UX · The centred garment's name is shown beneath the carousel. It connects photograph to word, confirms the tap landed, and gives the row somewhere to resolve — without it the carousel is just pictures moving.
2026-08-01 · studio UX · One primary action ("Save this outfit", full width, disabled under two pieces) instead of three equal pills. Shuffle and Start over are quiet text actions near the stage. A screen with three equally-weighted buttons has no primary action.
2026-08-01 · tooling · Added Playwright (playwright-core, driving the installed Edge) in the scratchpad to screenshot the running web build at 360/390/430pt. Reasoning about flex from source is how the overlap bugs got in; looking at real layout is how they got found.
2026-08-01 · bugfix · Horizontal ScrollViews were stretching to fill the flex column, opening two large dead gaps in Studio. Fixed with explicit height + `flexGrow: 0, flexShrink: 0`.
2026-08-01 · bugfix · The stage laid the figure out in fixed pixels and clipped its own head and shoes below ~700pt of height. Now drawn at a fixed design size (230×400) and scaled by a measured `onLayout`, so it fits any screen.
2026-08-01 · bugfix · Tab labels clipped the descender on "Today", rendering "Todav". `tabBarLabelStyle.lineHeight` did not fix it on web — React Navigation's label container was clipping. Now rendered with our own component.
2026-08-01 · bugfix · `initialScrollIndex` on FlashList v2 (to open the carousel on the current selection) breaks the first render pass — every card mounts blank. Removed, with a note so nobody re-adds it.
2026-08-01 · mobile · Removed the 900ms loading hold. Making the brand moment visible read as slowness, which is worse than being unseen.
2026-08-01 · studio · **Stage rebuilt as a ghost mannequin** — the fashion-photography term for a garment shown as if worn by an invisible person. Head, arms, torso, legs and feet as body-shaped regions; garments clipped into them, with one image spanning several regions so a top runs continuously across torso and both sleeves. Replaces free-floating rectangles. Vertical light wash and a contact shadow instead of a hard-edged arch.
2026-08-01 · assets · ⚠️ Cutout experiment (corner flood fill, `scratchpad/cutout.py` → `assets/garments/cutout/`): bottoms, shoes, hats and 2 bags cut out cleanly; **every top failed**. Tops are shot on white *wood*, whose plank texture and shadows stop a flood fill. Unresolved — a garment worn on a figure needs a transparent cutout, and this is now the single biggest blocker to the stage looking real.
2026-08-02 · assets · Backgrounds removed with **U2Net segmentation (rembg)** after the flood-fill approach failed on every top. `rembg` + `onnxruntime` are dev-machine asset-prep tools only — not app dependencies, never shipped, never called at runtime.
2026-08-02 · assets · The whole app now uses transparent cutouts rather than framed photographs. Two payoffs: garments can hang on the figure, and every surface shows them on the app's own colour — which removes the mismatched backgrounds that made the grid read as stock. PNG→WebP with alpha cut 2552 KB to 342 KB (-87%).
2026-08-02 · studio · With cutouts the stage no longer clips garments into body-shaped holes — the garment's own silhouette *is* the shape. Zones are just areas to fit inside, always `contentFit: contain` (a cutout cropped with `cover` is a garment with its sleeves cut off). A faint silhouette sits behind so the stage reads as a person when undressed.
2026-08-02 · design · Added `surfaceGarment` (light `#EDEBE8`, dark `#232020`), deeper than `surfaceMuted`. A white shirt on a near-white card is invisible, and most wardrobes are full of white shirts.
2026-08-02 · bugfix · Store default layer ids were stale after the garment set was renumbered — `bottom` pointed at a leather jacket and `footwear` at a tote, so the figure wore a jacket for trousers. Ids in `src/store/outfit.ts` must track `src/mock/data.ts`.
2026-08-02 · ux · Research on this app category is blunt: the slower the onboarding, the more likely someone abandons before cataloguing enough clothes for the app to be useful. Cataloguing must never feel like a second job. That reframed the priority — the biggest gap was not styling, it was that **there was no way to add a garment at all**.
2026-08-02 · ux · Added `app/add.tsx`: three large routes (photo, gallery, paste a link) and nothing else. Each states how long it takes, because nobody starts a chore they think will take an evening. Maps to F3 and F4.
2026-08-02 · ux · Add is reachable from the Studio header and the wardrobe grid, as a filled button — it is the app's primary action and belongs anywhere you might notice something missing.
2026-08-02 · ux · Three-step walkthrough, persisted with AsyncStorage so it appears once, replayable any time from the "?" in the Studio header. Skip is available on the first frame, never delayed or hidden. Three steps because the research says length is what kills completion.
2026-08-02 · ux · Onboarding store exposes `hydrated` and treats `seenWalkthrough: null` as unseen only after storage resolves. Without the hydration gate the walkthrough flashes on every launch before storage is read.
2026-08-02 · ux · Empty states teach rather than report. "Nothing here" tells someone what they can already see; the useful version says what to do next and gives them the control to do it. Filtering to nothing is a different problem from owning nothing, so it gets different copy and a different action.
2026-08-02 · payments · RevenueCat SDK integrated (`react-native-purchases` + `react-native-purchases-ui` v10.7). Entitlement `Vastra Pro`; products `lifetime`, `yearly`, `monthly`.
2026-08-02 · payments · ⚠️ **This conflicts with PROJECT.md §6**, which specifies `slots_3` / `slots_10` consumables plus `wardrobe_plus_monthly` / `wardrobe_plus_annual` under entitlement `plus`. §6's slot-pack model was the HAMM argument — buy the space you need, ads never involved. The new model is a straight Pro subscription. Built as instructed; **§6 needs amending or the products need revisiting.** Unresolved.
2026-08-02 · payments · Both RevenueCat packages are loaded via dynamic `import()`, never at module scope. Expo Go ships a fixed set of native modules and RevenueCat is not among them — a top-level import crashes the host app at start, as a hard native crash rather than a catchable error. `purchasesSupported()` gates on `Constants.executionEnvironment`, so Expo Go and web degrade to "purchases unavailable" with a reason the UI can show.
2026-08-02 · payments · The hosted RevenueCat paywall is tried first, with our own `/paywall` screen as the fallback. Hosted means pricing and copy change from the dashboard without an app release, which is what makes a price experiment possible inside the submission window (T40). The fallback keeps the §6 craft — loss framing, real close button, no countdowns — so degrading is never a dead end.
2026-08-02 · payments · `presentPaywallIfNeeded` rather than `presentPaywall`: it checks the entitlement first, so a subscriber who taps a gated action is never shown a paywall for something they already own.
2026-08-02 · payments · Customer Center used rather than hand-built subscription management. It covers cancellation, refunds, plan changes and restore consistently across both stores — the flows that are tedious to build and expensive to get wrong.
2026-08-02 · payments · API key lives only in `.env` (gitignored) and is read through `EXPO_PUBLIC_REVENUECAT_TEST_KEY`. RevenueCat SDK keys are public by design, but the repository is public, so the value stays out of source. Store-specific keys take precedence; the Test Store key is the fallback so the paywall is exercisable before store products exist.
2026-08-02 · payments · The client still never grants entitlements. It mirrors what RevenueCat reports; PROJECT.md §5.4 keeps the server authoritative for what an entitlement grants. The webhook and reconciliation (T28/T29) remain the source of truth for slots.
2026-08-02 · builds · Development build required from here on: `react-native-purchases` is a native module and Expo Go ships a fixed set that excludes it. Documented in docs/BUILDS.md.
2026-08-02 · builds · **EAS Build rather than a local Android build.** This machine has no JDK, no Android SDK and no adb, so building locally means several GB of downloads before the first compile. §3 already specifies EAS Build for CI, so the cloud path is both lighter and the one the spec calls for. `eas.json` defines development (APK + dev client), preview (APK, for Play closed testing) and production (AAB) profiles.
2026-08-02 · builds · `runtimeVersion` uses the `appVersion` policy and `appVersionSource: remote`, so EAS owns the version code. Hand-managed version codes drift the moment two people build.
2026-08-02 · scope · Confirmed with Ujjwal: **full backend as §3 specifies** — Clerk auth, R2, worker pipeline, webhook and reconciliation. Local-first was offered as a lower-risk alternative and declined.
2026-08-02 · builds · EAS project renamed: `eas init` had attached the app to a project called "ujjwal" and rewritten the slug to match. The CLI has no rename command, so a new project `@ujjwalxs-team/vastra` was created and linked (`eab4ffd8`). The old "ujjwal" project is now orphaned on the account and can be deleted from the dashboard.
2026-08-02 · builds · Added `.easignore`. EAS archives the working directory, so uncommitted work IS included — which is what we want here — but without this the archive carries node_modules, the API, the worker and every build artefact. With it the upload is 1.2 MB.
2026-08-02 · builds · Removed the `runtimeVersion: appVersion` policy. It requires expo-updates, which is not installed; leaving it set warns on every build and would silently mismatch runtimes if OTA updates were added later.
2026-08-02 · builds · The Android keystore is generated and held by EAS. Losing it means being unable to update an app already on the Play Store, so it must not be regenerated once a release exists.
2026-08-02 · outfits · Saving works. `src/store/savedOutfits.ts` persists outfits locally so work survives a restart; the server takes over as source of truth at T21/T23. Layers are **copied** on save, not referenced — the Studio keeps mutating afterwards, and a saved outfit must stay the thing you decided on.
2026-08-02 · outfits · The paywall fires at exactly one moment: attempting to save with no space left. Pro removes the cap entirely (`spaces.left === null` rather than a large number pretending to be a limit).
2026-08-02 · outfits · Deleting an outfit is always available and confirmed with a dialog that says it frees a space. §4 is explicit that the scarcity mechanic is the number of spaces, never the ability to remove your own data (UK GDPR Art. 17).
2026-08-02 · outfits · "Wear" loads a saved outfit back into the Studio rather than opening a read-only view — the most common thing to do with a saved outfit is make a variation of it.
2026-08-02 · outfits · Naming happens in a sheet before saving, pre-filled with the weekday. A saved outfit you cannot tell apart from four others is not really saved; pre-filling keeps the fast path at one tap.
2026-08-02 · studio · **Garment photographs removed from the figure.** Flat product shots pasted onto a body read as collage — wrong proportions, wrong lighting, and a folded flat-lay never looks worn. Garments now show as colour on the body, which preserves the thing that actually matters when composing an outfit: whether the combination works. Interim until the dedicated draped-outfit API exists.
2026-08-02 · home · "What others are wearing" is now a real horizontal scroll whose end card reveals smoothly as you reach it, tied to scroll position. An infinite list that quietly stops is a dead end; an end card that arrives as you get there turns it into an expectation, and gives the Notify me action somewhere honest to live. Still locked per §2.2 — no Buy or Unlock CTA anywhere on it.
2026-08-02 · feed · ⚠️ **Built against §2.3's cut list**, at Ujjwal's explicit direction. §2.2 had the OOTD feed and likes/comments as LOCKED UI, and §2.3 cut video upload, moderation, comments and the social graph outright. A working feed with user posts is now built. **§2.2 and §2.3 need rewriting** — the spec and the app now disagree about what V1 is.
2026-08-02 · feed · Full-screen vertical paging feed (`app/feed.tsx`). Posts are real: camera, gallery, or an outfit composed in the Studio, persisted locally and surviving restart. Not multi-user — that needs R2, auth and the API, none of which have credentials yet.
2026-08-02 · feed · **Moderation is enforced in the read path, not per screen.** `useVisiblePosts()` filters to `moderation === 'passed'`, so a future screen cannot accidentally render unreviewed content. Apple Guideline 1.2 and Google's UGC policy both require filtering before content is visible, and "we forgot on one screen" is the standard way apps fail that review.
2026-08-02 · feed · Every post carries Report (or Delete, if it is yours). Both stores require a reporting mechanism on user-generated content. **Still outstanding for store approval: a published EULA with zero tolerance for objectionable content, and user blocking.**
2026-08-02 · search · Visual search sits behind `SimilarSearchProvider` with a local stand-in. The real implementation must be a **server** call: an image-search key in the app bundle is a key anyone can extract and spend, and results need affiliate links attached, which is server work. Google Cloud Vision Product Search is the natural fit — Vertex AI is already the try-on provider, so it is one GCP project and one credential.
2026-08-02 · search · Match confidence is shown as coarse bands ("Very close" / "Close" / "Similar"), never a percentage. Users read "92% match" as a promise, and visual similarity cannot keep it.
2026-08-02 · feed · Camera and photo permissions are requested at the moment of use, never at launch. A permission prompt before someone knows why is the fastest route to a permanent no.
2026-08-02 · bugfix · "Trending isn't working" was literal — the controls were dead. `SectionHeader`'s action rendered a Pressable with no `onPress`, brand cards had a Pressable with no handler, and item detail's primary button had neither a handler nor a URL to open. All wired.
2026-08-02 · ux · `SectionHeader` now renders its action only when an `onAction` is supplied. An action with nowhere to go is worse than no action at all: it reads as broken software rather than as an absent feature.
2026-08-02 · shop · Re-added Discover as `app/shop.tsx` (F9). It was removed as a tab because it duplicated the Trending row; it returns as a destination, which is what "Shop" and "All" now open. Catalogue items gained `retailerUrl`, so "View at retailer" honours the two-taps-to-buy promise in F10.
2026-08-02 · item detail · "Add to an outfit" became "Wear this", which sets the layer and opens the Studio — the point of tapping a piece you own is to see it on. Categories with no Studio layer (dress, swimwear, underwear) are checked and explained rather than cast with `as never`, which would silently write a layer that cannot render.
2026-08-02 · bugfix · `useVisiblePosts` filtered **inside** the Zustand selector, returning a new array on every call. Zustand compares selector results by identity, so every render looked like a change — "getSnapshot should be cached", then "Maximum update depth exceeded". Fixed by selecting the raw array (stable reference) and deriving with `useMemo`. Audited every other selector in the app: no others return fresh objects or arrays.
2026-08-02 · dark theme · Rebuilt the dark palette. Two faults: bg/surface/surfaceMuted sat within a few hex points so cards never separated from the background (dark needs *more* separation than light — the eye discriminates poorly at the bottom of the range), and `surfaceGarment` was darker than `surface`, mirroring light mode. That is backwards: light needs a deeper card so a white shirt reads, dark needs a lighter one so a black jacket does. `surfaceGarment` went #232020 → #3D3936.
2026-08-02 · feed · Promoted to a first-class tab, **Looks**, second position — one tap from launch rather than buried behind a section header. "What others are wearing" was also just too long to be a label.
2026-08-02 · feed · Reels patterns adopted: full-bleed 9:16 paging, **double-tap to like** with a heart that scales and flies out (the 2011 Instagram gesture — it removed aiming from the engagement loop, which is why every platform copied it), gradient scrims rather than flat panels, and an icon action rail on the right edge. Double-tap only ever *adds* a like; un-liking by accident mid-double-tap is the classic bug in this pattern.
2026-08-02 · revenuecat · Added **Targeting by Placement**. Three named placements — `outfit_limit`, `profile_upsell`, `looks_feed` — each able to serve a different Offering from the dashboard. Intent differs wildly at those three moments (someone who just built an outfit and cannot save it is not the same buyer as someone browsing a feed), and one price for all of them leaves money on the table. Falls back to the current offering when Targeting is unconfigured, so the paywall never fails closed.
2026-08-02 · revenuecat · Added **subscriber attributes** (platform, app version). These are what Targeting and Experiments segment on — without them every user looks identical and audience targeting can do nothing. Deliberately non-identifying: no email or name until there is an account and consent.
2026-08-02 · revenuecat · ⚠️ **Virtual Currency resolves the §6 conflict.** RevenueCat now supports selling one-time packs of credits alongside subscriptions, which is exactly §6's original slot-pack model. Vastra can have both: `Vastra Pro` for unlimited spaces, and consumable credit packs for people who want three more without subscribing. Not yet built — needs dashboard configuration first.
2026-08-02 · hosting · ⚠️ **§3 specifies Fly.io, which is no longer free.** Fly.io and Railway both moved to trial/usage pricing. Four of the five §3 infrastructure choices (Neon, Upstash, R2, Clerk) remain genuinely free; only the application host needs changing. See docs/HOSTING.md.
2026-08-02 · hosting · Recommended Render free tier for the API, with the caveat that it spins down after 15 minutes and cold-starts in 30–60s. Mitigated by keeping the app local-first for everything on the critical path — the wardrobe, Studio, outfits and feed already work with the server off, so a cold start never blocks what a judge looks at.
2026-08-09 · slots · ⚠️ **Deletion-locking was proposed and rejected; the revised model is lawful.** The ask was "user can have 5 slots but can only delete outfit from one slot, remaining 4 unlocked after payment". That charges for erasure and contradicts §4 verbatim. The agreed replacement charges for *creation* instead: 1 **reusable** space (save/delete/re-save forever) plus 4 **single-use** credits, spent at save time and not refunded on delete. Deletion stays free, immediate and unconditional in both cases, so UK GDPR Art. 17 is untouched, while the commercial pressure the original idea wanted is preserved.
2026-08-09 · slots · The credit is spent at **save**, never held against the **delete**. This ordering is the whole legal argument and is not a style choice — `applyDelete` has no failure mode and no entitlement check by construction.
2026-08-09 · slots · Disclosure is part of the mechanic, not decoration. `SaveOutfitSheet` names the cost *above* the button and the button itself reads "Use a single-use save". A consumable discovered after the fact is a dark pattern; the same consumable priced up front is just a price — and Shipaton judges assess paywall craft.
2026-08-09 · slots · `singleUseSpent` is a counter that never decrements, rather than a count of surviving outfits. Deliberate: it lets a deleted outfit be erased **completely**, instead of being retained purely to keep a balance honest. Retaining data to run a counter would undermine the erasure right the whole design exists to protect.
2026-08-09 · slots · The rule lives in `@vastra/shared/slots` and is imported by both the mobile store and (next) the API, with 11 unit tests. The client runs it to *predict* a charge so it can disclose it; the server runs it to *enforce*. One implementation means the two can never disagree about what a user was charged. Tests cover the permanent-space-before-credits ordering (getting it backwards silently burns credits), ten delete/re-save cycles through the permanent space, and corrupt persisted ledgers on a rooted device degrading to "no space" rather than to free saves.
2026-08-09 · auth · **Firebase Auth replaces Clerk (§3 substitution #3).** Free at this scale and coherent with Vertex AI already being the try-on provider — one Google project, one billing account.
2026-08-09 · auth · The API verifies Firebase ID tokens against **Google's public JWKS**, needing only `FIREBASE_PROJECT_ID` and **no service-account key**. `firebase-admin` with a service account would give this process the power to mint a token for any user; token verification needs none of that. The API therefore holds no auth secret at all.
2026-08-09 · schema · Dropped the `slots` table. Spaces are now three counters on `users` plus `outfits.slot_kind`, which removes an entire table and the reconciliation between it and outfits. `slot_grants` is the append-only audit that makes those counters *explainable* — "why do I have three permanent spaces" must be reconstructable from rows, not asserted.
2026-08-09 · schema · `referrals.referred_user_id` is UNIQUE and a CHECK forbids self-referral, both enforced by the database rather than by application care. `rewarded_at` is set only after the referred account qualifies — if account creation were itself the payout, the cheapest way to farm permanent spaces is a burner email.
2026-08-09 · schema · `users.phone_hash` became nullable with a **partial** unique index. Firebase sign-in with Apple or Google yields no phone number, so the §4 F1 invariant ("one account per number") can only be enforced for accounts that have one. An unconditional unique index would have collided every phone-less account on null.
2026-08-09 · schema · Collapsed the migration baseline rather than carrying a rename-vs-create migration. `0000_init` had only ever been applied to a local dev container, and drizzle-kit's interactive disambiguation prompt cannot run in this shell. Both CHECK constraints and both partial indexes were verified present in the emitted SQL.
2026-08-09 · commerce · ⚠️ **Doorstep delivery cannot use in-app purchase.** Apple requires physical goods to be sold outside IAP, so RevenueCat cannot process it. More importantly, owning fulfilment means inventory, shipping, returns and cross-border VAT — a retail business, not a feature, on an 8-week clock. Affiliate (the existing "Shop this look" path, §2.3's Awin/CJ) delivers an identical user experience without the warehouse.
2026-08-09 · auth · Firebase project **vastra-bd3ba** created by the user. Sign-in method: **Google only**, chosen deliberately over Apple/email/phone. Two consequences recorded so they are not rediscovered late: (a) App Store guideline **4.8 makes Sign in with Apple mandatory** before iOS release once any third-party login is offered — buildable in an hour, but blocked on an Apple Developer account (T01); (b) there is **no fallback sign-in**, so a Google outage or a misconfigured SHA-1 during the demo shoot leaves no way into the app.
2026-08-09 · auth · Chose `@react-native-firebase` over the Firebase JS SDK. Native token persistence and Google Sign-In work properly on a dev build, where the JS SDK needs reCAPTCHA gymnastics and has had recurring React Native persistence regressions. Cost: a new EAS build, and Expo Go can no longer sign in — the same constraint RevenueCat already imposes.
2026-08-09 · auth · `useAuth` calls **`Purchases.logIn(uid)`** on sign-in and `logOut()` on sign-out. Until now RevenueCat issued anonymous ids, so a purchase belonged to an *install*: reinstall and it was gone. Aliasing to the Firebase uid is what lets an entitlement follow someone to a new phone. Best-effort and non-fatal — a RevenueCat failure must never block signing in.
2026-08-09 · auth · The guard is registered as **`APP_GUARD`**, so every route is closed unless it says `@Public()` or `@OptionalAuth()`. Opt-out, not opt-in: a forgotten decorator then produces a locked endpoint (a bug report) rather than an open one (a breach). `@OptionalAuth` exists for the Looks feed, which serves signed-out browsers but must know who is looking to mark their own likes.
2026-08-09 · auth · **Unconfigured must not mean unprotected.** With `FIREBASE_PROJECT_ID` absent the verifier is null and authenticated routes answer 503 rather than waving tokens through. Unit-tested, because this is precisely the failure that looks like success in production.
2026-08-09 · auth · A JWKS outage answers **503, not 401** — including on optional routes. Telling users their login is invalid because Google was briefly unreachable sends them round a re-authentication loop that cannot help. An *expired* token on an optional route does degrade to anonymous, so a stale session does not empty a public feed.
2026-08-09 · auth · 27 tests. The two that justify the file: a token minted for a **different Firebase project** is signed by the same Google keys and would verify perfectly on signature alone — rejected on `iss`/`aud`; and the `alg: none` forgery — rejected by pinning RS256. Tokens are signed with a real locally-generated key rather than stubbed, since the whole job is rejecting subtly-wrong tokens and only a genuine signing round-trip produces those.
2026-08-09 · auth · `google-services.json` and `GoogleService-Info.plist` are gitignored and supplied to EAS as file secrets. Not strictly secret, but there is no reason to publish them.
2026-08-09 · auth · `eas credentials` is interactive-only — no `--non-interactive` flag exists — so the Android SHA-1 must be read from the expo.dev credentials page rather than scripted.
2026-08-09 · auth · Verified the Firebase console's Gradle instructions are **not applicable**. Vastra uses Continuous Native Generation (no `android/` directory), and the `@react-native-firebase/app` config plugin injects `com.google.gms.google-services` and copies the JSON to `app/google-services.json` at prebuild — confirmed by reading the plugin's own constants rather than assuming.
2026-08-09 · auth · ⚠️ Inspected the downloaded `google-services.json`: correct project and package, and it contains the **Web** OAuth client (so Google is enabled in the console) but **no Android OAuth client**, because no SHA-1 has been added. `GoogleSignin.signIn()` fails with `DEVELOPER_ERROR` (status 10) in this state. The file must be re-downloaded after the fingerprint is added.
2026-08-09 · auth · ⚠️ **Google Play App Signing will require a second SHA-1.** Play re-signs uploads with its own key, so the fingerprint that works for EAS dev builds is not the one production installs use. Google Sign-In will work perfectly in testing and fail for every Play Store installer until the Play App Signing SHA-1 is also added to Firebase. Recorded now because the symptom appears only after release.
2026-08-09 · referrals · Referral alphabet excludes every confusable pair (`0/O`, `1/I/L`, `2/Z`, `5/S`, `8/B`) and all vowels — the latter removes accidental profanity by construction instead of via a blocklist, which is unwinnable across languages.
2026-08-09 · referrals · **`normaliseReferralCode` deliberately does not fold confusables.** The first version mapped `O→Q` and `1→7` so a misread would still resolve; that is wrong, because every such mapping is a guess and a wrong guess lands on a *different valid code* and credits a stranger. Rejecting is the safe failure. Caught in review before it shipped.
2026-08-09 · users · Provisioning happens on the **first authenticated request**, not via a separate sign-up call. Removes the class of bug where a client dies between authenticating and registering, leaving a valid token with no row behind it.
2026-08-09 · users · Referral-code collisions are handled by the unique index plus a retry loop, never by SELECT-then-INSERT, which is racy. The loop also re-reads on a uid collision, so two concurrent first-launch requests converge on one account instead of spinning.
2026-08-09 · users · `/v1/me` takes no id and reads identity only from `@CurrentUser`. An endpoint that accepts a user id and trusts it is the standard shape of an IDOR.
2026-08-09 · users · `toProfile` reports `isPro: false` until RevenueCat reconciliation lands (T29). Fails **closed** — an unverified entitlement is treated as absent rather than granted.
2026-08-09 · build · `@vastra/db` and `@vastra/shared` are consumed from their built `dist`, so a schema change is invisible to the API until those packages are rebuilt. Hit this immediately: the API still saw `clerkId` after the Firebase migration. Worth a turbo dependency rather than remembering.
2026-08-09 · sign-in · **The screen is not a gate.** The wardrobe, Studio, outfits and feed all work without an account, so "Continue without an account" is a first-class option rather than a hidden escape. Demanding an account before anyone has seen the app is the most reliable way to lose them — and the reason to sign in is stated concretely ("your wardrobe follows you to a new phone") instead of the usual contentless "sign in to continue".
2026-08-09 · sign-in · Framer Motion is web-only; **Moti** is its React Native equivalent (same declarative API, on Reanimated) and was already in the project. Entrance is a 110ms stagger down the screen, which leads the eye to the button instead of presenting everything at once.
2026-08-09 · sign-in · Installed `react-native-svg` to draw the **real Google "G"** in its four colours. Google's branding guidelines require the actual mark on a "Sign in with Google" control; a monochrome stand-in or a letter in a circle is a breach that gets caught at review rather than in testing. The button's white surface is also fixed by those guidelines and deliberately does *not* follow our theme in dark mode.
2026-08-09 · aurora · ⚠️ **First implementation looked bad and was rebuilt.** Concentric translucent rings were used to fake a radial falloff without a blur dependency; on screen they banded into visible arcs, and five overlapping garment swatches summed to grey mud. Replaced with `react-native-svg` `RadialGradient` — genuinely smooth falloff, GPU-rendered, and cheaper than the rings were. Verified by screenshot, not by reading the source.
2026-08-09 · aurora · The tints are **not** the garment swatches. Using them directly was the obvious idea and failed: those colours are deliberately desaturated so garments read as cloth, and overlapping muted browns and greens produces grey. The aurora uses the same hues pushed up in saturation, which reads as light through fabric rather than as dye.
2026-08-09 · aurora · Removed the full-screen gradient scrim. It was there to guarantee text contrast but desaturated the entire screen to flat grey — it protected legibility by destroying what it was protecting. Contrast now comes from capping orb opacity instead.
2026-08-09 · aurora · Orb intensity is halved in dark mode. Warm saturated tints over a near-black ground turn brown and dirty at full strength; dark wants a suggestion of colour, not the same wash dimmed.
2026-08-09 · aurora · Animates `translate`/`scale` only, never layout properties — the same rule that fixed the LoadingScreen crash, where an infinite Reanimated loop on `width` re-laid out the tree every frame and killed the host app.
2026-08-09 · tooling · Expo's typed routes regenerate from the **dev server**, not from `expo export`. A new screen fails typecheck with "not assignable to parameter of type ..." until `expo start` has run once. Cost a confusing failure; recorded so the next new route does not.
2026-08-09 · bugfix · ⚠️ **`webClientId: 'autoDetect'` was invented and would have broken sign-in.** No such sentinel exists in `@react-native-google-signin/google-signin` v16 — `webClientId` is a plain optional string, so `'autoDetect'` would have been passed to `requestIdToken()` as a literal client id. Verified against `Utils.java`: `requestIdToken` is called **only** when `webClientId` is non-empty, so omitting it returns a signed-in user with a null `idToken` and nothing to exchange for a Firebase credential. The real web client id (client_type 3) now comes from `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`, and an absent value fails loudly at configure rather than silently at the token exchange.
2026-08-09 · bugfix · ⚠️ **Google error codes were matched by substring and never fired on Android.** `code.includes('CANCEL')` assumed the literal name, but on native `statusCodes` comes from `NativeModule.getConstants()` and Android reports cancellation as `"12501"` — no recognisable word at all. Every user who simply backed out of the account picker would have been shown an error. Now compared against `statusCodes.SIGN_IN_CANCELLED` / `PLAY_SERVICES_NOT_AVAILABLE` / `IN_PROGRESS`.
2026-08-09 · verification · Both bugs above were invisible to TypeScript, because both are *values* of correctly-typed strings. Typecheck validates shape, never meaning — magic strings must be checked against the installed package or a live endpoint. The Firebase JWKS URL was verified the same way (live, 4 RS256 keys) rather than trusted from memory.
2026-08-09 · walkthrough · Moved from above the root `Stack` into `(tabs)/_layout`. It teaches the tab experience, so covering a modal the user deliberately opened — sign-in, the paywall, add-a-piece — was simply wrong. Placing it inside the tabs makes that **impossible by construction**: every modal and pushed screen is a sibling of `(tabs)` in the root Stack and renders above it. A runtime route check would also have worked and would have been one refactor away from breaking again. Verified by screenshotting `/sign-in` with nothing dismissed, and confirming the tab bar still renders.
2026-08-09 · auth · Added `retryable` to the auth store. A "Try again" is offered only when retrying could plausibly succeed — a thrown setup error. On web and in Expo Go the native module is structurally absent, and a retry button that can never work is worse than none.
2026-08-09 · tooling · `pnpm check:google` validates google-services.json before a build wastes twenty minutes on EAS: right package, web client present and matching `.env`, and at least one Android OAuth client. It also warns when only one SHA-1 is registered, since Play App Signing needs a second one.
2026-08-16 · palette · ⚠️ **Removed the amber accent from both themes.** Dark's `#FBBF24` was a hard saturated yellow the user disliked on sight; light's `#A16207` was the same hue. Replaced with a clay-rose family — dark `#DDA898`, light `#9C5A44` (5.3:1 on white, passes AA). Accent surfaces now render a two-stop **gradient** (`accentGradient`) rather than a flat fill: a single saturated block is what made the amber read as harsh, and a short warm→cool travel gives the same surface depth. Kept to two stops on a diagonal — a vertical ramp on a pill reads as a 2010 bevel.
2026-08-16 · palette · `accentGradient` is a design token, not a per-screen style, so the gradient cannot drift between the paywall, the outfit-limit panel and the Plus row. `Button` swaps its container for a `LinearGradient` only on `variant="accent"`; primary actions stay monochrome, per the standing rule that colour in this app means premium.
2026-08-16 · legal · ⚖️ **Virtual try-on is the largest legal exposure in the product, and it is not hypothetical.** Illinois BIPA carries a private right of action with $1,000/$5,000 statutory damages per person. Virtual try-on is an active litigation area: Charlotte Tilbury settled a try-on class action for $2.925M, and a comparable case against MAC survived a motion to dismiss in June 2026. Those concern face geometry; a full-body photo used to fit garments is at minimum arguable. Three options recorded in docs/legal/RISK.md — geo-restrict IL/TX/WA, build full BIPA compliance, or never send a body photo at all (which is what the app does today). **Must be decided before try-on is built, not after.**
2026-08-16 · legal · Drafted docs/legal/PRIVACY.md and TERMS.md against UK GDPR + DPA 2018, CCPA/CPRA, and BIPA — including the publicly-available biometric retention and destruction schedule BIPA requires (destroy at withdrawal, account deletion, purpose satisfied, or 3 years from last interaction, whichever is soonest). Both are drafts and explicitly need a solicitor before publication.
2026-08-16 · legal · TERMS.md carries the Apple 1.2 requirements verbatim in substance: zero-tolerance statement for objectionable content, automated pre-screening, 24-hour report handling, and user blocking. **None of those three mechanisms is built yet** — the Looks feed cannot ship until they are, and publishing a policy that promises them first would itself be a misrepresentation.
2026-08-16 · legal · docs/legal/RISK.md ends with a promise-to-implementation table, because a policy claiming behaviour the app lacks is an FTC Act §5 deception risk in its own right. Four rows are ❌ today: account deletion, data export, automatic biometric destruction, and report/block.
2026-08-16 · ux · Research findings to apply (not yet applied): first meaningful action inside 60s roughly doubles D7 retention; 3–5 onboarding screens maximum; delay sign-up until necessary (already true — sign-in is not a gate); and reduce the number of *decisions* rather than the number of taps.
2026-08-16 · onboarding · **The walkthrough now ends in an action, not a dismissal.** It already met the "3–5 screens, skippable, no sign-up gate" bar, but failed the finding that matters most — reaching a first meaningful action inside ~60s roughly doubles D7 retention. The old final button read "Let's go" and dropped the user on a browse screen, so onboarding finished with three slides *read* and nothing *done*. The last step is now "Build your first outfit" and opens the Studio, where the figure is already dressed: one tap, no camera permission, no photography before it pays off.
2026-08-16 · onboarding · Copy rewritten as things you can do rather than a feature list, and shortened — every sentence in onboarding is a sentence standing between someone and the product. One action per step, never a choice between two: reducing the number of *decisions* matters more than reducing taps.
2026-08-16 · bugfix · ⚠️ Walkthrough step 3 still promised "Five spaces free… delete any of them any time", which the slot model made false. Corrected to one permanent space plus four single-use saves. Onboarding copy is a promise; leaving it stale would have been a misrepresentation of a paid mechanic.
2026-08-16 · bugfix · ⚠️ **Profile displayed a hardcoded "4 of 5 spaces used"** regardless of actual state, and a hardcoded `@ujjwal` handle. Now reads the real ledger and the signed-in account. A fabricated number on a settings screen is worse than no number — it teaches users the app's own reporting cannot be trusted.
2026-08-16 · bugfix · ⚠️ **A white garment was invisible on the Studio stage.** The stage drew `surfaceMuted → surface` (#F5F5F4 → #FFFFFF) and the white swatch is #F5F5F4, so a cotton sweatshirt disappeared and the figure read as half-dressed. Found only because the new onboarding CTA lands there — the "aha" moment was showing a naked mannequin. The stage now uses `surfaceGarment`, the token that exists for exactly this, and garment strokes moved from `border` to `borderStrong`. Verified in both themes: white reads on light, and the same change keeps black reading on dark.
2026-08-16 · paywall · "All five slots are full" → "Every space is full", and "Plus" → "Pro". The app says *spaces* everywhere else, and the entitlement is `Vastra Pro`; the paywall was the last place still using the old vocabulary.
2026-08-16 · taxonomy · **Added a department axis** (`womenswear | menswear | kids | unisex`) to `@vastra/shared`, the Drizzle schema, and the catalogue. A clothing app with no way to say who a garment is cut for cannot filter, search, or recommend usefully — this was a foundational gap, not a missing screen.
2026-08-16 · taxonomy · Called **department**, not gender, deliberately. It is a retail merchandising axis — the section of a shop an item sits in — not a claim about the person browsing. Multi-select, because buying for yourself, a partner and a child is one session rather than three, and forcing one choice would make the app wrong for those people rather than merely unhelpful.
2026-08-16 · taxonomy · `unisex` is returned by **every** department filter, which is the bug this design exists to prevent: shop Menswear and every plain tee catalogued as unisex silently disappears. An empty selection means *everything*, not nothing — a filter that hides the whole catalogue reads as a broken app. Both rules are unit-tested in `departments.test.ts`.
2026-08-16 · taxonomy · Owned wardrobe items default to `unisex`. We have no business assigning a department to clothes someone already owns and wears; the axis exists for shopping, not for the user's own things.
2026-08-16 · brands · **Brand cards now open a brand page.** Every one of them previously pushed the same generic `/shop`, which made the whole "From brands" row decorative — you could look at a brand and then not reach it. New `app/brand/[slug].tsx` shows that brand's catalogue, composing with the department filter.
2026-08-16 · brands · Used expo-router's object form (`{ pathname: '/brand/[slug]', params }`) rather than a template literal. Typed routes cannot narrow `` `/brand/${string}` `` for a dynamic segment, and the object form is both type-safe and the documented approach.
2026-08-16 · ads · ⚖️ **`sponsored` is a field on the data, not a styling choice, and renders a visible "AD" label.** Paid placements must be disclosed under the UK CAP Code and FTC endorsement guidance; a flag the renderer cannot ignore is the only version that survives a redesign. Disclosed twice — on the card, and as "Paid partnership" on the destination, so arriving from a paid placement never loses that context.
2026-08-16 · catalogue · ⚠️ The brand page looks sparse because the mock catalogue has only one Studio Nord item. Not a code fault — it is T05/T24 (seed catalogue selection and ingestion) still being unstarted. Real brand pages need roughly 12–20 items each before they read as shops.
2026-08-16 · search · Added a **catalogue query engine** to `@vastra/shared` — ranking, price bands, size matching and sort, with 21 unit tests. Pure and shared for the same reason the slot rule is: the client filters what it has in memory, the server will build a query from it, and one implementation means search cannot rank differently online and offline.
2026-08-16 · search · Search is **AND across terms, not OR.** "black jacket" returning every black item *and* every jacket is the classic bad search, and it is worse than no search because it looks like it worked. Field weights encode intent — title beats brand beats colour — and whole-word beats prefix beats substring, so "tee" ranks a tee above a "canteen".
2026-08-16 · search · Searches the wardrobe **and** the catalogue together. "Where are my black jeans" and "I want black jeans" are the same question typed the same way; making someone pick a corpus first is a decision the app can make for them.
2026-08-16 · catalogue · Expanded the mock catalogue **4 → 20 items** across four brands, all four departments and all three price bands, with real size runs. Four items made every filter untestable and every brand page look broken. Still mock — T05/T24 unchanged.
2026-08-16 · sizes · Sizes are **per scale**, not one list: apparel letters, waist numbers, shoe numbers. A single scale is a lie and is what makes retail size filters useless. Unsized categories (bags, sunglasses) and items with no size data always match — hiding something because data is *missing* is worse than showing it.
2026-08-16 · filters · Price uses three **bands**, not a two-handle slider. A slider asks someone to invent two numbers; bands ask them to recognise one. Same reason the filter sheet shows a live result count: nobody should have to guess whether a choice emptied the screen.
2026-08-16 · wishlist · Saved pieces are **unlimited**, deliberately. Outfit spaces are the scarcity mechanic because that is where the work and the value are; charging for a bookmark on something someone intends to buy would be taxing them for wanting to spend money.
2026-08-16 · bugfix · ⚠️ **"Delete account" was a dead control** — it rendered, looked real, and had no `onPress`. Same class as the earlier "trending isn't working". Both stores require a working in-app deletion path and UK GDPR Article 17 requires erasure, so this was a hard release blocker sitting in plain sight.
2026-08-16 · deletion · `deleteEverything()` enumerates AsyncStorage keys by prefix rather than deleting from a hand-written list — a list is exactly what goes stale next time someone adds a store. It also resets in-memory Zustand state, because clearing storage while a store still holds the data means the next render writes it straight back. Partial failures are reported honestly instead of showing a success screen over data that is still there.
2026-08-16 · deletion · Remote identity is deleted **first**. If Firebase refuses, we stop before wiping the device, because local data is then the only way to recover the account. Firebase's `requires-recent-login` is surfaced as a plain instruction rather than an error code.
2026-08-16 · deletion · The screen states that **deleting an account does not cancel a Play subscription**. Someone who deletes and keeps being charged has a genuine grievance, and burying that would be the kind of dark pattern regulators actually look for. Confirmation is one explicit step — friction to prevent accidents, never to change minds. No retention offer, no discount, no typing a phrase.
2026-08-16 · moderation · ⚠️ **Blocking a post is not blocking a person.** The store only had `block(id)`, which hides one post; Google Play's UGC policy and Apple 1.2 both require blocking an abusive *user*, and reporting their posts one at a time is not that. Added `blockAuthor`/`unblockAuthor`, persisted, enforced in the read path beside the moderation gate so no screen can accidentally render a blocked author's content.
2026-08-16 · moderation · Blocked accounts are listed and reversible in Profile. A block with no way back is a support ticket waiting to happen.
2026-08-23 · legal · In-app Terms & Privacy (`app/legal.tsx` + `src/legal/content.ts`), synced with `docs/legal/{TERMS,PRIVACY}.md` and `docs/legal/index.html`, on branch `docs/legal-docs`. Sign-in links open the in-app page; creators named as Diksha Nigam and Ujjwal Deep; contact vastra.sup@gmail.com.
2026-08-23 · legal · **Deferred until the app is complete (not blocking current work):** (4) host `docs/legal/index.html` on a public HTTPS URL for Play Store / App Store listing; (5) ICO data-protection fee self-assessment and registration if required; (6) solicitor review before treating the policies as final. In-app copy and repo drafts ship first; store publication and regulatory/legal sign-off wait for a finished product.
2026-08-23 · docs · Restored the original README language (Sanskrit etymology, “wardrobe in your pocket”, problem/solution framing from before the pitch rewrite) — preferred over the later status-led rewrite on this branch.
